# API Configuration
# Leave empty for automatic detection (localhost in dev, relative in prod)
# Or set to your API server URL: https://your-api-domain.com/api
VITE_API_BASE_URL=

# Development API URL (used when VITE_API_BASE_URL is empty and not in production)
# VITE_API_BASE_URL=http://localhost:3000/api

# Production API URL (used when VITE_API_BASE_URL is empty and in production)
# Will use '/api' (relative to frontend domain)